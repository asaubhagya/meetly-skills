interface:
  display_name: "Set up Meetly"
  short_description: "Personalize notes and daily briefings"
  brand_color: "#151515"
  default_prompt: "Use $setup-meetly to set up Meetly."
dependencies:
  tools:
    - type: "mcp"
      value: "meetly"
      description: "Retrieve authorized Meetly transcript evidence"
      transport: "streamable_http"
      url: "https://mcp.getmeetly.ai/mcp"
policy:
  allow_implicit_invocation: true
