interface:
  display_name: "Set up Meetly"
  short_description: "Personalize meeting digests and recurring briefings"
  brand_color: "#ED5A36"
  default_prompt: "Use $setup-meetly to help me get started and tailor my meeting digests."
dependencies:
  tools:
    - type: "mcp"
      value: "meetly"
      description: "Retrieve authorized Meetly meeting evidence"
      transport: "streamable_http"
      url: "https://mcp.getmeetly.ai/mcp"
policy:
  allow_implicit_invocation: true
